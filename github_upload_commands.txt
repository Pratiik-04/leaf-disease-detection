Run these commands inside the project folder after installing Git:

git init
git add .
git commit -m "Add leaf disease detection ML project"
git branch -M main
git remote add origin https://github.com/Pratiik-04/leaf-disease-detection.git
git push -u origin main
